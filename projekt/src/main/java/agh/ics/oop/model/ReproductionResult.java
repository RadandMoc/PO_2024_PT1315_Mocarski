package agh.ics.oop.model;

import java.util.List;

public record ReproductionResult(List<Byte> genome, List<Animal> parents) { // nie rozumiem tej klasy, czy wynikiem rozmnażania nie jest po prostu zwierzę?
}
